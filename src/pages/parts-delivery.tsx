import { useEffect, useMemo, useState } from 'react';
import { getApp, getApps, initializeApp } from 'firebase/app';
import { get, getDatabase, ref } from 'firebase/database';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
import { FirebaseService } from '@/services/firebase';
import { getLang, type Lang } from '@/lib/i18n';

type Ticket = Record<string, any>;
type OpenPoItem = { po_number?: string; po_item?: string; part?: string; openqty?: number; orderdate?: string };
type OpenPoExtraItem = { actualShipmentDate?: string };
type SalesOrderDetailItem = { description: string; material: string; orderQty: number; deliveryCount: number | null };
type DetailTicket = { id: string; ageDays: number; ticket: Record<string, any> };
type OpenPoDetail = { poNumber: string; poItem: string; part: string; openQty: number; status: 'intransit' | 'notshipped'; ageingDays: number };
type Row = { key: string; material: string; description: string; totalQty: number; totalTickets: number; oldestAging: number; stockQty: number; openPoQty: number; details: DetailTicket[]; openPoDetails: OpenPoDetail[] };
const PAGE_SIZE = 20;
const ticketFirebaseConfig = { apiKey: import.meta.env.VITE_FIREBASE_API_KEY || '', authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN || '', databaseURL: import.meta.env.VITE_FIREBASE_DATABASE_URL || '', projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID || '', storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET || '', messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID || '', appId: import.meta.env.VITE_FIREBASE_APP_ID || '' };
const app = getApps().some((a) => a.name === 'ticketsApp') ? getApp('ticketsApp') : initializeApp(ticketFirebaseConfig, 'ticketsApp');
const ticketDb = getDatabase(app);
const parseTicketDate = (raw?: string) => { if (!raw) return null; const iso = new Date(raw); if (!Number.isNaN(iso.getTime())) return iso; const m = raw.match(/^(\d{2})\/(\d{2})\/(\d{4})$/); if (!m) return null; return new Date(Number(m[3]), Number(m[2]) - 1, Number(m[1])); };
const getAgeDays = (ticketEntry: Ticket) => { const createdOn = parseTicketDate(String(ticketEntry?.ticket?.CreatedOn || '')); if (!createdOn) return 0; return Math.max(0, Math.floor((Date.now() - createdOn.getTime()) / 86400000)); };
const parseSalesOrderDetails = (value: unknown): SalesOrderDetailItem[] => { if (!value) return []; let parsed: unknown = value; if (typeof value === 'string') { try { parsed = JSON.parse(value); } catch { return []; } } if (Array.isArray(parsed)) return parsed.flatMap((item) => parseSalesOrderDetails(item)); if (typeof parsed !== 'object' || parsed === null) return []; const entries = Object.entries(parsed as Record<string, unknown>); const looksLikeItem = entries.some(([key]) => ['Delivery Count', 'Description', 'Material', 'Order Qty'].includes(key)); if (looksLikeItem) { const item = parsed as Record<string, unknown>; const deliveryCount = Number.parseInt(String(item['Delivery Count'] ?? item.deliveryCount ?? ''), 10); const orderQty = Number.parseFloat(String(item['Order Qty'] ?? item.orderQty ?? '0')); return [{ description: String(item.Description ?? item.description ?? '-'), material: String(item.Material ?? item.material ?? '-').trim(), deliveryCount: Number.isNaN(deliveryCount) ? null : deliveryCount, orderQty: Number.isNaN(orderQty) ? 0 : orderQty }]; } return entries.flatMap(([, nestedValue]) => parseSalesOrderDetails(nestedValue)); };

export default function PartsDeliveryPage() {
  const [lang, setLang] = useState<Lang>(getLang()); const [tickets, setTickets] = useState<Record<string, Ticket>>({}); const [search, setSearch] = useState(''); const [currentPage, setCurrentPage] = useState(1); const [stockByMaterial, setStockByMaterial] = useState<Record<string, number>>({}); const [openPoByMaterial, setOpenPoByMaterial] = useState<Record<string, number>>({}); const [openPoDetailsByMaterial, setOpenPoDetailsByMaterial] = useState<Record<string, OpenPoDetail[]>>({});
  useEffect(() => { const fn = () => setLang(getLang()); window.addEventListener('language-change', fn); return () => window.removeEventListener('language-change', fn); }, []);
  useEffect(() => {
    Promise.all([get(ref(ticketDb, 'c4cTickets_test/tickets')), FirebaseService.getAllParts(), get(ref(getDatabase(), 'production_report/open_po/items')), get(ref(getDatabase(), 'app_admin/openpo_vendor_3060_extra'))]).then(([ticketSnap, allParts, openPoSnap, openPoExtraSnap]) => {
      setTickets((ticketSnap.val() || {}) as Record<string, Ticket>);
      setStockByMaterial(Object.entries(allParts || {}).reduce<Record<string, number>>((acc, [material, part]) => { acc[String(material).trim()] = Number((part as { Current_Stock_Qty?: number })?.Current_Stock_Qty || 0); return acc; }, {}));
      const extraMap = (openPoExtraSnap.val() || {}) as Record<string, OpenPoExtraItem>;
      const detailMap: Record<string, OpenPoDetail[]> = {};
      const qtyMap = Object.values((openPoSnap.val() || {}) as Record<string, OpenPoItem>).reduce<Record<string, number>>((acc, item) => {
        const material = String(item.part || '').trim(); if (!material) return acc;
        const po = String(item.po_number || '').trim(); const poItem = String(item.po_item || '').trim(); const extraKey = `${po}__${poItem}__${material}`.replace(/[.#$\[\]/]/g, '_');
        const status = extraMap[extraKey]?.actualShipmentDate ? 'intransit' : 'notshipped'; if (status !== 'notshipped') return acc;
        const openQty = Number(item.openqty || 0); acc[material] = (acc[material] || 0) + openQty;
        const orderDate = item.orderdate ? new Date(item.orderdate) : null; const ageingDays = orderDate && !Number.isNaN(orderDate.getTime()) ? Math.max(0, Math.floor((Date.now() - orderDate.getTime()) / 86400000)) : 0;
        detailMap[material] = detailMap[material] || []; detailMap[material].push({ poNumber: po, poItem, part: material, openQty, status, ageingDays });
        return acc;
      }, {});
      setOpenPoByMaterial(qtyMap); setOpenPoDetailsByMaterial(detailMap);
    });
  }, []);

  const rows = useMemo(() => { const map = new Map<string, Row>(); Object.values(tickets).forEach((entry) => { const ticketData = entry?.ticket || {}; const status = String(ticketData.TicketStatusText || ticketData.TicketStatus || '').toLowerCase(); const rejectionStatus = String(ticketData['Order Rejection Status'] || '').trim(); if (status === 'closed' || rejectionStatus === 'Fully Rejected') return; const ageDays = getAgeDays(entry); parseSalesOrderDetails(ticketData['Sales Order Details']).filter((item) => item.deliveryCount === 0).forEach((item) => { if (!item.material) return; const key = `${item.material}__${item.description}`; const current = map.get(key) || { key, material: item.material, description: item.description, totalQty: 0, totalTickets: 0, oldestAging: 0, stockQty: 0, openPoQty: 0, details: [], openPoDetails: [] }; current.totalQty += item.orderQty || 1; const tid = String(ticketData.TicketID || '-'); if (!current.details.some((x) => x.id === tid)) current.details.push({ id: tid, ageDays, ticket: ticketData }); current.totalTickets = current.details.length; current.oldestAging = Math.max(current.oldestAging, ageDays); current.stockQty = stockByMaterial[item.material] || 0; current.openPoQty = openPoByMaterial[item.material] || 0; current.openPoDetails = openPoDetailsByMaterial[item.material] || []; map.set(key, current); }); }); return Array.from(map.values()).sort((a, b) => b.oldestAging - a.oldestAging); }, [tickets, stockByMaterial, openPoByMaterial, openPoDetailsByMaterial]);
  const filtered = useMemo(() => { const k = search.trim().toLowerCase(); if (!k) return rows; return rows.filter((r) => `${r.material} ${r.description}`.toLowerCase().includes(k)); }, [rows, search]);
  const totalPages = Math.max(1, Math.ceil(filtered.length / PAGE_SIZE)); const pagedRows = useMemo(() => filtered.slice((currentPage - 1) * PAGE_SIZE, currentPage * PAGE_SIZE), [filtered, currentPage]); useEffect(() => setCurrentPage(1), [search]); useEffect(() => { if (currentPage > totalPages) setCurrentPage(totalPages); }, [currentPage, totalPages]);
  return <div className="space-y-6"><h1 className="text-3xl font-bold">{lang === 'zh' ? 'Parts Delivery（全部，deliveryCount=0）' : 'Parts Delivery (All, deliveryCount=0)'}</h1><Card><CardHeader><CardTitle>{lang === 'zh' ? '搜索' : 'Search'}</CardTitle></CardHeader><CardContent><Input value={search} onChange={(e) => setSearch(e.target.value)} placeholder={lang === 'zh' ? '按物料号或描述搜索' : 'Search material or description'} /></CardContent></Card><Card><CardContent className="overflow-auto pt-6"><table className="min-w-full text-sm"><thead><tr className="border-b"><th className="p-2 text-left">Material Code</th><th className="p-2 text-left">Description</th><th className="p-2 text-left">Total Qty</th><th className="p-2 text-left">Ticket Count</th><th className="p-2 text-left">Oldest Aging (days)</th><th className="p-2 text-left">Stock Qty</th><th className="p-2 text-left">Open PO Qty</th><th className="p-2 text-left">Detail</th></tr></thead><tbody>{pagedRows.map((r) => <tr key={r.key} className="border-b"><td className="p-2">{r.material}</td><td className="p-2">{r.description}</td><td className="p-2">{r.totalQty}</td><td className="p-2">{r.totalTickets}</td><td className="p-2">{r.oldestAging}</td><td className="p-2">{r.stockQty}</td><td className="p-2"><Dialog><DialogTrigger asChild><Button variant="outline" size="sm">{r.openPoQty}</Button></DialogTrigger><DialogContent className="max-w-3xl"><DialogHeader><DialogTitle>{r.material} Open PO</DialogTitle></DialogHeader><table className="min-w-full text-xs"><thead><tr className="border-b"><th className="p-2 text-left">PO</th><th className="p-2 text-left">Item</th><th className="p-2 text-right">Qty</th><th className="p-2 text-right">Ageing</th><th className="p-2 text-left">Status</th></tr></thead><tbody>{r.openPoDetails.map((d, i) => <tr key={`${d.poNumber}_${d.poItem}_${i}`} className="border-b"><td className="p-2">{d.poNumber || '-'}</td><td className="p-2">{d.poItem || '-'}</td><td className="p-2 text-right">{d.openQty}</td><td className="p-2 text-right">{d.ageingDays}d</td><td className="p-2">{d.status === 'intransit' ? 'In Transit' : 'Not Shipped'}</td></tr>)}</tbody></table></DialogContent></Dialog></td><td className="p-2"><Dialog><DialogTrigger asChild><Button variant="outline" size="sm">Detail</Button></DialogTrigger><DialogContent className="max-w-5xl"><DialogHeader><DialogTitle>{r.material} - {r.description}</DialogTitle></DialogHeader><div className="max-h-[70vh] overflow-auto"><table className="min-w-full text-xs"><thead><tr className="border-b"><th className="p-2 text-left">TicketID</th><th className="p-2 text-left">Ageing</th><th className="p-2 text-left">SO Created Date</th></tr></thead><tbody>{r.details.sort((a,b)=>b.ageDays-a.ageDays).map((d)=> <tr key={d.id} className="border-b"><td className="p-2">{d.ticket.TicketID || d.id}</td><td className="p-2">{d.ageDays}</td><td className="p-2">{d.ticket['SO Created Date'] || '-'}</td></tr>)}</tbody></table></div></DialogContent></Dialog></td></tr>)}</tbody></table></CardContent></Card></div>;
}
